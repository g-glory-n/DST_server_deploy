[NETWORK]
server_port = 11000            # 监听的 udp 端口，只能介于 10998 - 11018 之间，确保每个实例都不同。

[SHARD]
is_master = true               # 是否为 master 服务器，只能存在一个 true，其他为 false。

[STEAM]
master_server_port = 27018     # steam 用的端口，确保每个实例都不同。
authentication_port = 8768     # steam 用的端口，确保每个实例都不同。
